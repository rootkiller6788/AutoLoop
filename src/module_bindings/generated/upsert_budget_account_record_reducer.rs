// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertBudgetAccountRecordArgs {
    pub account_key: String,
    pub tenant_id: String,
    pub principal_id: String,
    pub policy_id: String,
    pub total_budget_micros: u64,
    pub reserved_micros: u64,
    pub spent_micros: u64,
    pub blocked_count: u64,
    pub updated_at_ms: u64,
}

impl From<UpsertBudgetAccountRecordArgs> for super::Reducer {
    fn from(args: UpsertBudgetAccountRecordArgs) -> Self {
        Self::UpsertBudgetAccountRecord {
            account_key: args.account_key,
            tenant_id: args.tenant_id,
            principal_id: args.principal_id,
            policy_id: args.policy_id,
            total_budget_micros: args.total_budget_micros,
            reserved_micros: args.reserved_micros,
            spent_micros: args.spent_micros,
            blocked_count: args.blocked_count,
            updated_at_ms: args.updated_at_ms,
        }
    }
}

impl __sdk::InModule for UpsertBudgetAccountRecordArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_budget_account_record`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_budget_account_record {
    /// Request that the remote module invoke the reducer `upsert_budget_account_record` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_budget_account_record:upsert_budget_account_record_then`] to run a callback after the reducer completes.
    fn upsert_budget_account_record(
        &self,
        account_key: String,
        tenant_id: String,
        principal_id: String,
        policy_id: String,
        total_budget_micros: u64,
        reserved_micros: u64,
        spent_micros: u64,
        blocked_count: u64,
        updated_at_ms: u64,
    ) -> __sdk::Result<()> {
        self.upsert_budget_account_record_then(
            account_key,
            tenant_id,
            principal_id,
            policy_id,
            total_budget_micros,
            reserved_micros,
            spent_micros,
            blocked_count,
            updated_at_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_budget_account_record` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_budget_account_record_then(
        &self,
        account_key: String,
        tenant_id: String,
        principal_id: String,
        policy_id: String,
        total_budget_micros: u64,
        reserved_micros: u64,
        spent_micros: u64,
        blocked_count: u64,
        updated_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_budget_account_record for super::RemoteReducers {
    fn upsert_budget_account_record_then(
        &self,
        account_key: String,
        tenant_id: String,
        principal_id: String,
        policy_id: String,
        total_budget_micros: u64,
        reserved_micros: u64,
        spent_micros: u64,
        blocked_count: u64,
        updated_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertBudgetAccountRecordArgs {
                account_key,
                tenant_id,
                principal_id,
                policy_id,
                total_budget_micros,
                reserved_micros,
                spent_micros,
                blocked_count,
                updated_at_ms,
            },
            callback,
        )
    }
}
