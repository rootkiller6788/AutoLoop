// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::spend_ledger_kind_type::SpendLedgerKind;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AppendSpendLedgerRecordArgs {
    pub id: String,
    pub tenant_id: String,
    pub account_key: String,
    pub session_id: String,
    pub trace_id: String,
    pub task_id: String,
    pub capability_id: String,
    pub kind: SpendLedgerKind,
    pub amount_micros: i64,
    pub token_cost_micros: u64,
    pub tool_cost_micros: u64,
    pub duration_cost_micros: u64,
    pub reason: String,
    pub created_at_ms: u64,
}

impl From<AppendSpendLedgerRecordArgs> for super::Reducer {
    fn from(args: AppendSpendLedgerRecordArgs) -> Self {
        Self::AppendSpendLedgerRecord {
            id: args.id,
            tenant_id: args.tenant_id,
            account_key: args.account_key,
            session_id: args.session_id,
            trace_id: args.trace_id,
            task_id: args.task_id,
            capability_id: args.capability_id,
            kind: args.kind,
            amount_micros: args.amount_micros,
            token_cost_micros: args.token_cost_micros,
            tool_cost_micros: args.tool_cost_micros,
            duration_cost_micros: args.duration_cost_micros,
            reason: args.reason,
            created_at_ms: args.created_at_ms,
        }
    }
}

impl __sdk::InModule for AppendSpendLedgerRecordArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `append_spend_ledger_record`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait append_spend_ledger_record {
    /// Request that the remote module invoke the reducer `append_spend_ledger_record` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`append_spend_ledger_record:append_spend_ledger_record_then`] to run a callback after the reducer completes.
    fn append_spend_ledger_record(
        &self,
        id: String,
        tenant_id: String,
        account_key: String,
        session_id: String,
        trace_id: String,
        task_id: String,
        capability_id: String,
        kind: SpendLedgerKind,
        amount_micros: i64,
        token_cost_micros: u64,
        tool_cost_micros: u64,
        duration_cost_micros: u64,
        reason: String,
        created_at_ms: u64,
    ) -> __sdk::Result<()> {
        self.append_spend_ledger_record_then(
            id,
            tenant_id,
            account_key,
            session_id,
            trace_id,
            task_id,
            capability_id,
            kind,
            amount_micros,
            token_cost_micros,
            tool_cost_micros,
            duration_cost_micros,
            reason,
            created_at_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `append_spend_ledger_record` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn append_spend_ledger_record_then(
        &self,
        id: String,
        tenant_id: String,
        account_key: String,
        session_id: String,
        trace_id: String,
        task_id: String,
        capability_id: String,
        kind: SpendLedgerKind,
        amount_micros: i64,
        token_cost_micros: u64,
        tool_cost_micros: u64,
        duration_cost_micros: u64,
        reason: String,
        created_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl append_spend_ledger_record for super::RemoteReducers {
    fn append_spend_ledger_record_then(
        &self,
        id: String,
        tenant_id: String,
        account_key: String,
        session_id: String,
        trace_id: String,
        task_id: String,
        capability_id: String,
        kind: SpendLedgerKind,
        amount_micros: i64,
        token_cost_micros: u64,
        tool_cost_micros: u64,
        duration_cost_micros: u64,
        reason: String,
        created_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AppendSpendLedgerRecordArgs {
                id,
                tenant_id,
                account_key,
                session_id,
                trace_id,
                task_id,
                capability_id,
                kind,
                amount_micros,
                token_cost_micros,
                tool_cost_micros,
                duration_cost_micros,
                reason,
                created_at_ms,
            },
            callback,
        )
    }
}
