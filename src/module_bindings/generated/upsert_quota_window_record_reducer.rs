// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertQuotaWindowRecordArgs {
    pub window_key: String,
    pub tenant_id: String,
    pub account_key: String,
    pub window_start_ms: u64,
    pub window_end_ms: u64,
    pub window_budget_micros: u64,
    pub consumed_micros: u64,
    pub blocked_count: u64,
    pub updated_at_ms: u64,
}

impl From<UpsertQuotaWindowRecordArgs> for super::Reducer {
    fn from(args: UpsertQuotaWindowRecordArgs) -> Self {
        Self::UpsertQuotaWindowRecord {
            window_key: args.window_key,
            tenant_id: args.tenant_id,
            account_key: args.account_key,
            window_start_ms: args.window_start_ms,
            window_end_ms: args.window_end_ms,
            window_budget_micros: args.window_budget_micros,
            consumed_micros: args.consumed_micros,
            blocked_count: args.blocked_count,
            updated_at_ms: args.updated_at_ms,
        }
    }
}

impl __sdk::InModule for UpsertQuotaWindowRecordArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_quota_window_record`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_quota_window_record {
    /// Request that the remote module invoke the reducer `upsert_quota_window_record` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_quota_window_record:upsert_quota_window_record_then`] to run a callback after the reducer completes.
    fn upsert_quota_window_record(
        &self,
        window_key: String,
        tenant_id: String,
        account_key: String,
        window_start_ms: u64,
        window_end_ms: u64,
        window_budget_micros: u64,
        consumed_micros: u64,
        blocked_count: u64,
        updated_at_ms: u64,
    ) -> __sdk::Result<()> {
        self.upsert_quota_window_record_then(
            window_key,
            tenant_id,
            account_key,
            window_start_ms,
            window_end_ms,
            window_budget_micros,
            consumed_micros,
            blocked_count,
            updated_at_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_quota_window_record` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_quota_window_record_then(
        &self,
        window_key: String,
        tenant_id: String,
        account_key: String,
        window_start_ms: u64,
        window_end_ms: u64,
        window_budget_micros: u64,
        consumed_micros: u64,
        blocked_count: u64,
        updated_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_quota_window_record for super::RemoteReducers {
    fn upsert_quota_window_record_then(
        &self,
        window_key: String,
        tenant_id: String,
        account_key: String,
        window_start_ms: u64,
        window_end_ms: u64,
        window_budget_micros: u64,
        consumed_micros: u64,
        blocked_count: u64,
        updated_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertQuotaWindowRecordArgs {
                window_key,
                tenant_id,
                account_key,
                window_start_ms,
                window_end_ms,
                window_budget_micros,
                consumed_micros,
                blocked_count,
                updated_at_ms,
            },
            callback,
        )
    }
}
