// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertCostAttributionRecordArgs {
    pub id: String,
    pub tenant_id: String,
    pub principal_id: String,
    pub policy_id: String,
    pub session_id: String,
    pub trace_id: String,
    pub task_id: String,
    pub capability_id: String,
    pub provider_tokens: u32,
    pub tool_invocations: u32,
    pub duration_ms: u64,
    pub token_cost_micros: u64,
    pub tool_cost_micros: u64,
    pub duration_cost_micros: u64,
    pub total_cost_micros: u64,
    pub settled_at_ms: u64,
}

impl From<UpsertCostAttributionRecordArgs> for super::Reducer {
    fn from(args: UpsertCostAttributionRecordArgs) -> Self {
        Self::UpsertCostAttributionRecord {
            id: args.id,
            tenant_id: args.tenant_id,
            principal_id: args.principal_id,
            policy_id: args.policy_id,
            session_id: args.session_id,
            trace_id: args.trace_id,
            task_id: args.task_id,
            capability_id: args.capability_id,
            provider_tokens: args.provider_tokens,
            tool_invocations: args.tool_invocations,
            duration_ms: args.duration_ms,
            token_cost_micros: args.token_cost_micros,
            tool_cost_micros: args.tool_cost_micros,
            duration_cost_micros: args.duration_cost_micros,
            total_cost_micros: args.total_cost_micros,
            settled_at_ms: args.settled_at_ms,
        }
    }
}

impl __sdk::InModule for UpsertCostAttributionRecordArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_cost_attribution_record`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_cost_attribution_record {
    /// Request that the remote module invoke the reducer `upsert_cost_attribution_record` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_cost_attribution_record:upsert_cost_attribution_record_then`] to run a callback after the reducer completes.
    fn upsert_cost_attribution_record(
        &self,
        id: String,
        tenant_id: String,
        principal_id: String,
        policy_id: String,
        session_id: String,
        trace_id: String,
        task_id: String,
        capability_id: String,
        provider_tokens: u32,
        tool_invocations: u32,
        duration_ms: u64,
        token_cost_micros: u64,
        tool_cost_micros: u64,
        duration_cost_micros: u64,
        total_cost_micros: u64,
        settled_at_ms: u64,
    ) -> __sdk::Result<()> {
        self.upsert_cost_attribution_record_then(
            id,
            tenant_id,
            principal_id,
            policy_id,
            session_id,
            trace_id,
            task_id,
            capability_id,
            provider_tokens,
            tool_invocations,
            duration_ms,
            token_cost_micros,
            tool_cost_micros,
            duration_cost_micros,
            total_cost_micros,
            settled_at_ms,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_cost_attribution_record` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_cost_attribution_record_then(
        &self,
        id: String,
        tenant_id: String,
        principal_id: String,
        policy_id: String,
        session_id: String,
        trace_id: String,
        task_id: String,
        capability_id: String,
        provider_tokens: u32,
        tool_invocations: u32,
        duration_ms: u64,
        token_cost_micros: u64,
        tool_cost_micros: u64,
        duration_cost_micros: u64,
        total_cost_micros: u64,
        settled_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_cost_attribution_record for super::RemoteReducers {
    fn upsert_cost_attribution_record_then(
        &self,
        id: String,
        tenant_id: String,
        principal_id: String,
        policy_id: String,
        session_id: String,
        trace_id: String,
        task_id: String,
        capability_id: String,
        provider_tokens: u32,
        tool_invocations: u32,
        duration_ms: u64,
        token_cost_micros: u64,
        tool_cost_micros: u64,
        duration_cost_micros: u64,
        total_cost_micros: u64,
        settled_at_ms: u64,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertCostAttributionRecordArgs {
                id,
                tenant_id,
                principal_id,
                policy_id,
                session_id,
                trace_id,
                task_id,
                capability_id,
                provider_tokens,
                tool_invocations,
                duration_ms,
                token_cost_micros,
                tool_cost_micros,
                duration_cost_micros,
                total_cost_micros,
                settled_at_ms,
            },
            callback,
        )
    }
}
